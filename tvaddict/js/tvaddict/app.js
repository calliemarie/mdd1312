var app = angular.module('TVAddictApp',[]);

//config([ '$routeProvider', function($routeProvider){

//	$routeProvider
//		when("/shows", {templateUrl: "partials/shows.html", controller: "mainController"}).
//		when("/show/:id", {templateUrl: "partials/show.html", controller: "mainController"}).
//		otherwise({redirectTo:'/shows'});
	
//}])